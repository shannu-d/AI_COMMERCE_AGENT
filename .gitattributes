# Normalize line endings so the repository content is identical on every platform.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary.
*.png binary
*.jpg binary
*.pdf binary
